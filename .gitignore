node_modules/
dist/
.vite/
.shots/
*.log
.DS_Store

# lokalny stan `wrangler dev` (SQLite Durable Objects, bundle) — artefakt runtime'u
.wrangler/
.dev.vars

# ustawienia Claude Code per-maszyna (launch.json zostaje, bo jest wspolny dla projektu)
.claude/settings.local.json
